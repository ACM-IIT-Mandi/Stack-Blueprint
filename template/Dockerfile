# Skeleton Dockerfile, replace with a real one for your stack.
# Requirements and rationale: docs/standards.md


FROM alpine:3.20
RUN echo "This blueprint has no Dockerfile yet. See docs/standards.md" && exit 1
